<?php

test('only administrators can access estimates and engagements page', function () {
    $this->markTestIncomplete();
});

test('estimate can be marked as returned', function () {
    $this->markTestIncomplete();
});

test('agreement can be marked as received', function () {
    $this->markTestIncomplete();
});
